import React, {Component} from 'react';

const Deck = (props) => {
      return(
        <div id="deck">
            <img src="images/cards/default/verso.png"/> 
        </div>
      );
}

export default Deck;